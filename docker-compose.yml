services:
  morgana:
    build:
      context: .
      target: production
    container_name: morgana
    image: asubowo/morgana:3.2
    restart: unless-stopped
    env_file: .env
    network_mode: bridge
